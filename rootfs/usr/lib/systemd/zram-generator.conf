# zram-generator — compressed in-RAM swap
# Docs: https://github.com/systemd/zram-generator
#
# zram-size = ram / 2  →  16 GB on a 32 GB system
# zstd offers the best compression ratio at low CPU cost.

[zram0]
zram-size = ram / 2
compression-algorithm = zstd

