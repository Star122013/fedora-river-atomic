[Journal]
# Persist journal to /var/log/journal so logs survive reboots.
# Essential for debugging bootc image boot failures.
Storage=persistent
# Sync to disk every 5s instead of the default 5min.
# Without this, a hard power-off after a black screen wipes the runtime journal from RAM.
SyncIntervalSec=5s
SystemMaxUse=5G
